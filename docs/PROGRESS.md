# PayPhone — Progress Log

This is an append-only log. Each milestone adds its own section.
**DO NOT delete or modify earlier entries.** Append only.

Format for new entries:

```
## M<n> — <title> (<date/time>)

### What was built
- Bullet list of files created or significantly changed

### What was tested (with proof)
- Bullet list of tests run and what passed
- Include tx hashes, screenshots paths, command outputs

### What is broken or deferred
- Anything not working, why, and the plan to fix

### What's next
- Single sentence: the next milestone or task
```

---

## M0 — Pre-flight (May 5–6, 2026)

Completed before any product code.

### What was set up

- CDP account created at `portal.cdp.coinbase.com`
- Ed25519 API key generated and rotated (after a one-time accidental disclosure of an earlier key, that key was deleted)
- Wallet Secret generated via portal Server Wallet → Accounts → Wallet Secret → Generate
- CDP buyer wallet created at `0xE01669A01E28E905055Ac6cD33c19ced7e10d870`
  - Created via `scripts/wallet-setup.ts` using `cdp.evm.getOrCreateAccount({ name: "payphone-buyer" })`
  - Idempotent — running the script twice returns the same address
- $5 USDC funded on Base mainnet
  - Sent by teammate from Coinbase Wallet (after they bought $2 of ETH on Coinbase Exchange to cover gas, then withdrew on Base network)
  - Verified at `https://basescan.org/address/0xE01669A01E28E905055Ac6cD33c19ced7e10d870`
  - Token contract `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` confirmed (real Circle USDC, $0.9998 price match)
- Daily.co account created at subdomain `payphone.daily.co`
- Daily.co API key obtained and saved to `.env.local`
- Daily.co $15 signup credit auto-applied (covers all expected hackathon transcription cost of ~$3)
- `.env.local` populated with: `CDP_API_KEY_ID`, `CDP_API_KEY_SECRET`, `CDP_WALLET_SECRET`, `DAILY_API_KEY`

### Folder cleanup

- Project folder renamed from `consensus/` to `payphone/`
- Old `node_modules/`, `package.json`, `package-lock.json`, `.gitignore` deleted (will be regenerated by `create-next-app`)
- `wallet-setup.ts` moved to `scripts/wallet-setup.ts` (preserved for reuse)
- `docs/` folder created with: `CONTEXT.md`, `PROGRESS.md`, `STRETCH_GOALS.md`, `RESEARCH_DOSSIER.md`

### What's next

**Milestone 1** — x402 round-trip on Base Sepolia using the `exact` scheme (hardcoded $0.10, no upto, no Daily, no UI). See `docs/EXECUTION_PROMPT.md` for the M1 brief.
